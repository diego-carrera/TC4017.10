"""
Setup file for the reservations_system package.
"""

from setuptools import setup, find_packages

setup(
    name="reservations_system",
    description="Simple hotel reservation system.",
    version="0.0.1",
    packages=find_packages(),
)
